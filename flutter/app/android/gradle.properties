# T20: host has 3.6GB RAM total - the Flutter template's default -Xmx8G OOMs here.
# -Xmx1g is the MANDATORY cap; daemon=false keeps a one-shot build from leaving
# a resident Gradle daemon behind; workers.max=2 bounds parallel worker memory.
org.gradle.jvmargs=-Xmx1g -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.daemon=false
org.gradle.workers.max=2
kotlin.daemon.jvmargs=-Xmx768m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
